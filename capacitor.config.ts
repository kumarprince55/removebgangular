import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.electionadvisor.in',
  appName: 'Election Advisor',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
